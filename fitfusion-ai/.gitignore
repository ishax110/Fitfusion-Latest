# Environment variables — never commit this
.env

# Python
__pycache__/
*.py[cod]
*.pyc
venv/
.venv/
dist/
build/
*.egg-info/

# Models (large binary files)
# .joblib files are committed so Railway Docker build can access them
# If the model grows beyond 100MB, switch to Git LFS

# IDE
.vscode/
.idea/
